---
- name: Copy nginx configuration file and Dockerfile
  ansible.builtin.template:
    dest: "{{ item.dest }}"
    src: "{{ item.src }}"
  loop:
    - dest: "{{ requirements_dir }}/nginx/conf/webserver.conf"
      src: webserver.conf.j2
    - dest: "{{ requirements_dir }}/nginx/Dockerfile"
      src: Dockerfile.j2